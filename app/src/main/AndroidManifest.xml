<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jesseliii.pragueia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RenterLoginActivity" />
        <activity android:name=".RenterProfile" />
        <activity android:name=".CustomerLoginActivity" />
        <activity android:name=".AddBus" />
        <activity android:name=".CustomerProfile" />
        <activity android:name=".RenterSettings" />
        <activity android:name=".ViewBus" />
        <activity android:name=".AddHistory" />
        <activity android:name=".ViewHistory" />
        <activity android:name=".ViewHistoryList"></activity>
    </application>

</manifest>